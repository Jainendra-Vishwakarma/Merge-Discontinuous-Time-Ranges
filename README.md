# Merge-Discontinuous-Time-Ranges
You are given an array of time ranges representing when a system was active. Each range is an array [start, end) where both start and end are UNIX timestamps in milliseconds.
